import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.arteconecta.io',
  appName: 'arteconecta',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
